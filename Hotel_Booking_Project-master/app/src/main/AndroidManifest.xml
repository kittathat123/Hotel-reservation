<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.user.hotel_booking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".room_and_fam_activity" />
        <activity android:name=".Info1" />
        <activity android:name=".Info2" />
        <activity android:name=".Info3" />
        <activity android:name=".roomselect" />
        <activity
            android:name=".pop1"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".pop2"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".pop3"
            android:theme="@style/AppTheme.CustomTheme" />

        <activity android:name=".FacilityList"></activity>

        <activity android:name=".invoice">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>




    </application>

</manifest>